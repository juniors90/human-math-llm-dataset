Let \(G\) be a nonempty finite set equipped with an associative binary operation.
Assume that for all \(a,b,c \in G\),
\[
ab = ac \implies b = c
\quad \text{and} \quad
ba = ca \implies b = c.
\]
Then \(G\) is a group.

Show that this conclusion may fail if \(G\) is infinite.
